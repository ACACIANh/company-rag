# 회사 RAG 챗봇 — 역기획서 (Reverse Spec)

> 목적: 현행 구현을 요구사항으로 역추론하고, **실제로 해결하려는 문제**와 **현황 진단·해결 과제**를 정리하여 향후 기획 추가·수정의 기반으로 삼는다.
> 기준 시점: 2026-06-01 / 기준 브랜치: `main` (PR #37 폴더 권한 모델 전환 직후)

---

## 1. 개요

**한 줄 정의**: 회사 내부 문서(Markdown)에 대해 부서 단위 접근 권한을 지키면서 멀티턴으로 질의응답하는, LangGraph 기반 RAG 챗봇.

**대상 사용자**: 사내 임직원(부서 소속). 자신이 속한 부서가 열람 권한을 가진 폴더의 문서에 대해서만 답변을 받는다.

**핵심 가치**:
- 흩어진 사내 문서를 자연어 질문으로 검색·요약 — "문서 어디 있는지" 찾는 비용 제거
- 부서 권한 경계를 검색 단계에서 강제 — 권한 없는 문서는 애초에 검색되지 않음
- Self-RAG(관련성 평가 + 환각 검증 + 재시도)로 "모르면 모른다" 하는 신뢰성

---

## 2. 해결하려는 문제 (Problem Statement)

### 2.1 사내 문서 검색의 페인포인트
사내 지식이 Markdown 문서로 폴더 트리에 흩어져 있다. 직원은 (a) 문서가 어디 있는지 모르고, (b) 키워드 검색으로는 맥락이 담긴 답을 못 얻으며, (c) 여러 문서를 종합해야 답이 나오는 질문에 시간을 쓴다. → **자연어 질의 → 종합 답변**이 필요하다.

### 2.2 권한·보안 요구
사내 문서에는 부서별로 접근이 제한되어야 하는 것이 섞여 있다. "검색 결과를 보여준 뒤 거른다"는 방식은 정보 유출 위험이 있다. → **권한 없는 문서는 검색 후보에 아예 포함되지 않아야** 한다(pre-filter).

### 2.3 환각·정확도 요구
LLM이 문서에 없는 내용을 그럴듯하게 지어내면, 사내 의사결정의 근거로 쓰일 때 위험하다. → 검색 문서의 **관련성을 평가**하고, 생성된 답변이 **근거 문서에 기반하는지 검증**하며, 부족하면 **재시도**하거나 "관련 문서를 찾지 못했다"고 **명시**해야 한다.

### 2.4 대화 맥락 요구
"그건 어떻게 해?" 같은 후속 질문은 직전 맥락 없이 검색이 안 된다. → **멀티턴 메모리 + 질문 재작성**(대명사 해소·맥락화)이 필요하다.

---

## 3. 목표 / 비목표

### Goals
- 부서 권한을 지키는 사내 문서 RAG Q&A
- 환각을 억제하는 Self-RAG 파이프라인
- 토큰 단위 스트리밍(SSE) 응답
- 세션 이력 저장·조회, 회귀 평가 자동화
- 인프라(LLM·임베더·벡터스토어·리트리버·리랭커·권한·세션) 교체 가능한 추상화

### Non-Goals (현 시점)
- 본격 에이전트(자율 도구 사용 루프) — 현재는 단일 도구 호출 + HITL 승인 골격만
- 실시간 웹검색 통합 — 그래프 경로 제거됨, 향후 에이전틱 도구로 재편 예정 (9.1 참조)
- 문서 포맷 다양화 — 현재 Markdown 인제스천만
- 개인(user) 단위 권한 — 권한 주체는 부서(department)뿐 (sensitivity·개인 메타데이터 폐기됨)

---

## 4. 사용자 & 핵심 시나리오

**페르소나**: 부서에 소속된 사내 직원. 로그인(JWT) 후 챗 UI로 질문.

**핵심 시나리오**
1. **단순 문서 질의**: "엔지니어링팀 온보딩 절차 알려줘" → 권한 폴더 검색 → 근거와 함께 답변 + 출처 인용.
2. **복합 질의**: "온보딩이랑 개발환경 셋업 둘 다" → multi-query 분해 → 병렬 검색 + RRF 병합 → 종합 답변.
3. **후속 질의**: "그럼 그건 누구한테 물어봐?" → chat_history로 재작성 후 검색.
4. **권한 밖 질의**: 권한 없는 폴더 내용 질문 → 검색 후보에 없어 "관련 문서를 찾지 못했습니다" 안내.
5. **도구 실행 요청(HITL)**: 부수효과 있는 도구 호출 → `interrupt()`로 사용자 승인 대기 → 승인 시 실행.

---

## 5. 기능 요구사항 (현행 구현 역매핑)

각 요구사항을 충족하는 LangGraph 노드/엣지를 함께 표기한다. 노드는 순수 함수(State in → State out), side effect는 `core/` 호출로만.

| # | 요구사항 | 구현 노드/위치 |
|---|----------|----------------|
| FR-1 | 직전 대화 맥락 로드(최대 10턴) | `load_memory` (`app/graph/nodes/load_memory.py`) |
| FR-2 | 질문 재작성(대명사 해소·맥락화) | `rewrite_query` (`app/graph/nodes/rewrite_query.py`) |
| FR-3 | 경로 라우팅 + 검색 전략 결정 | `router` (`app/graph/nodes/router.py`) — `route`∈{doc_search, tool_call}, `rewrite_strategy`∈{none, contextual, multi_query} |
| FR-4 | 부서 권한 폴더 조회 | `permission` (`app/graph/nodes/permission.py`) → `allowed_folders` |
| FR-5 | 복합 질문 하위 쿼리 분해(2~3개) | `multi_query` (`app/graph/nodes/multi_query.py`) |
| FR-6 | 권한 필터 + 다중 쿼리 병렬 검색 + RRF 병합 + 리랭킹 | `retrieve` (`app/graph/nodes/retrieve.py`) |
| FR-7 | 검색 문서 관련성 점수화(0.0~1.0) | `grade_documents` (`app/graph/nodes/grade_documents.py`) |
| FR-8 | 관련성 부족 시 질문 재작성 재시도(최대 2) | `increment_retry` + `route_after_grade` (`app/graph/edges.py`) |
| FR-9 | 근거 기반 답변 생성 + 출처 인용 + 스트리밍 | `generate` (`app/graph/nodes/generate.py`) |
| FR-10 | 답변 환각 검증(context 근거 여부) | `check_hallucination` (`app/graph/nodes/check_hallucination.py`) |
| FR-11 | 환각 시 재생성(총 재시도 3 상한) | `route_after_hallucination` (`app/graph/edges.py`) |
| FR-12 | 도구 호출 사용자 승인(HITL) | `confirm` (`interrupt()`) → `tool_executor` |
| FR-13 | 대화 이력 누적 저장 | `save_memory` + SessionStore |
| FR-14 | 문서 없음/저관련 시 명시적 안내 | `generate_node` (doc_search에서 문서 부재·점수<0.5 → 안내문) |

**그래프 흐름**
```
START → load_memory → rewrite_query → router ─┬─ doc_search ─→ permission → [multi_query?] → retrieve → grade_documents ─┬→ generate
                                              │                                                                          └→ increment_retry ↻ rewrite_query
                                              └─ tool_call ──→ confirm ─┬→ tool_executor → generate
                                                                        └→ END (미승인)
generate → check_hallucination ─┬→ save_memory → END
                                └→ generate ↻ (재생성)
```
빌더: `app/graph/builder.py` / 엣지: `app/graph/edges.py`

---

## 6. 비기능 요구사항

| # | 항목 | 요구 | 구현 |
|---|------|------|------|
| NFR-1 | 권한 | 권한 없는 문서는 검색 후보에서 제외(pre-filter) | OpenFGA 권한 모델(ADR-0015) + 가시 폴더 path 정확 매칭 WHERE 필터 (`core/fga/`) |
| NFR-2 | 성능(권한) | 매 검색마다 FGA 호출 비용 절감 | 사용자별 `allowed_folders` PostgreSQL TTL 캐시(기본 60s, Redis 미사용) |
| NFR-3 | 성능(검색) | path-prefix 매칭 고속화 | `path` 컬럼 text_pattern_ops 인덱스 + pgvector HNSW(코사인) |
| NFR-4 | 인증 | 사용자 식별·세션 격리 | JWT (`PyJWT`), 안전하지 않은 기본 시크릿 거부 |
| NFR-5 | 남용 방지 | 분당 요청 제한 | 사용자별 인메모리 rate limit (`RATE_LIMIT_PER_MINUTE`) |
| NFR-6 | 응답성 | 토큰 단위 실시간 응답 | SSE(`text/event-stream`) + `asyncio.Queue` token_queue |
| NFR-7 | 상태 지속성 | 멀티턴 메모리 + HITL 중단점 | LangGraph checkpointer — 운영 `AsyncPostgresSaver`, 기본 `MemorySaver` |
| NFR-8 | 품질 회귀 | 변경 시 검색 품질 추적 | `tests/eval/runner.py`(recall@k, keyword_hit_rate) + `POST /admin/eval/run`·`GET /admin/eval/results` |
| NFR-9 | 교체 가능성 | 인프라 구현체 교체 | `core/`의 ABC + Factory (LLM·Embedder·VectorStore·Retriever·Reranker·Session·PermissionCache) |
| NFR-10 | 비용 가시성 | LLM 사용 비용 추적·조회 | `GET /admin/cost/report` |

---

## 7. 아키텍처 개요

### 7.1 레이어 경계 (절대 위반 금지)
- `core/`: LangGraph를 모른다. LangGraph-불가지 공용 인프라. ABC + Factory로 구현체 교체 가능.
- `app/`: `core/`의 인터페이스(ABC)만 의존. LangGraph 워크플로우 + FastAPI API.
- `app/graph/nodes/`: 순수 함수. State in → State out. side effect는 `core/` 호출로만.
- `web/`: 백엔드 API로만 통신.

### 7.2 State 스키마
`AgentState(TypedDict)` — `MessagesState`·임의 dict 금지 (`app/graph/state.py`).

| 필드 | 타입 | 의미 |
|------|------|------|
| `question` / `rewritten_question` | str | 원본 / 재작성 질문 |
| `chat_history` | list[dict] | 대화 이력 |
| `route` | Literal[doc_search, tool_call] | 라우팅 결정 |
| `rewrite_strategy` | Literal[none, contextual, multi_query] \| None | 검색 전략 |
| `multi_queries` | list[str] | 분해된 하위 쿼리 |
| `documents` | list[SearchResult] | 검색 결과(Chunk+score) |
| `relevance_score` | float | 관련성 점수 |
| `retry_count` | int | 재시도 횟수 |
| `answer` | str | 생성 답변 |
| `citations` | list[SourceRef] | 인용 출처 |
| `hallucination_passed` | bool | 환각 검증 결과 |
| `confirmed` | bool | HITL 승인 여부 |
| `tool_input` | str | 도구 실행 요청 |
| `user_id` | str | 요청 사용자 |
| `allowed_folders` | list[str] | 권한 폴더(permission이 채움) |

### 7.3 core/ 추상화와 구현체

| 추상화(ABC) | 구현체 |
|-------------|--------|
| `LLMClient` (`complete`, `stream`) | `OpenAIClient`, `AnthropicClient` (+ Factory) |
| `Embedder` (`embed`, `embed_batch`) | `OpenAIEmbedder`, `SentenceTransformerEmbedder` |
| `VectorStore` (`add`, `search`, `count`) | `PostgresVectorStore` (pgvector) |
| `Retriever` (`retrieve`) | `BasicRetriever` / (웹) `TavilyRetriever`, `DuckDuckGoRetriever` |
| `Reranker` (`rerank`) | `NoOpReranker`, `RRFReranker`, `LLMReranker` |
| FGA | `FGAClient` (ListObjects → path-prefix 필터) + `PermissionCacheBackend`(`PostgresPermissionCache`) |
| `SessionStore` | `PostgresSessionStore`, `MemorySessionStore` |

> 미연결 구현체·어댑터라도 **학습 목적 + 교체 가능성**을 위해 보존(CLAUDE.md 규칙 5).

### 7.4 데이터 모델 (DTO)
`Document`, `Chunk`, `SearchResult(chunk, score)`, `SourceRef`, `Answer` (`core/models.py`).

### 7.5 권한 동작 흐름 (OpenFGA + path-prefix)
모델(ADR-0015): `user` / `role(member)` / `department(member)` / `folder(parent, dept_viewer·dept_access, public_viewer·public_access, private_flag, super_reader, can_read: super_reader or dept_access or (public_access but not private_flag))`. 정책은 **전체공개 기본 + private 서브트리 차단 + 부서 명시권한(dept_viewer) + 전사 상위 열람권(super_reader, role 기반)**. 폴더 트리는 단일 루트 `/company` 하위로 두며 코퍼스 루트 디렉토리명을 `base_path`로 주입(`docs/company`→`/company`). (모델 정의: `backend/fga/model.fga`·`backend/fga/model.json`)
1. `permission_node` → `ListObjects(user, can_read, folder)` → 가시 폴더 path 목록(상속까지 풀림)
2. `build_pg_filter` → `path = ANY($1)` **정확 매칭** WHERE 절(+params), 빈 목록이면 `FALSE`. prune·path-prefix는 쓰지 않는다 — private 하위 폴더가 상위(public) 권한으로 새지 않도록 (ADR-0015)
3. `retrieve_node` → 동일 필터로 검색, RRF 병합 후 상위 k 리랭킹

### 7.6 인제스천 파이프라인
`MarkdownLoader.load()` → `FixedSizeChunker.chunk(size=500, overlap=50)` → `Embedder.embed_batch()` → `PostgresVectorStore.add()`. 초기화: `scripts/build_index.py`, 재인덱싱: `POST /admin/index/rebuild`.

### 7.7 API 엔드포인트
- **챗** (`app/api/chat.py`): `POST /chat`(비스트리밍), `POST /chat/stream`(SSE)
- **인증** (`app/api/auth.py`): `POST /auth/token`, `GET /auth/me`
- **세션** (`app/api/sessions.py`): `GET /sessions`, `GET /sessions/{id}/messages`, `DELETE /sessions/{id}`
- **관리자** (`app/api/admin.py`): `GET /admin/index/status`, `POST /admin/index/rebuild`, `POST /admin/eval/run`, `GET /admin/eval/results`, `GET /admin/cost/report`, `GET /admin/users`, `POST·DELETE /admin/users/{user_id}/departments/{department_id}`(부서 추가·제거), `DELETE /admin/users/{user_id}`(사용자 삭제)

---

## 8. 기술 스택 & 의존성

**오케스트레이션**: `langgraph`, `langgraph-checkpoint-postgres`, `langchain`, `langchain-core`
**LLM**: `openai`, `anthropic`, `langchain-openai`, `langchain-anthropic`
**DB·벡터**: `psycopg`, `asyncpg`, `pgvector` (PostgreSQL 15)
**API**: `fastapi`, `uvicorn`
**권한·인증**: `openfga-sdk`, `PyJWT`, `python-dotenv`
**검색·임베딩**: `tavily-python`, `duckduckgo-search`, `sentence-transformers`
**설정**: `pyyaml`
**개발**: `pytest`(+mock, asyncio), `ruff`, `locust`
**인프라**: Docker Compose (PostgreSQL+pgvector, OpenFGA)

---

## 9. 현황 진단 & 해결하려는 부분 ★

향후 기획 추가·수정의 출발점. 각 항목은 **현황 → 문제 → 제안**으로 정리한다.

### 9.1 web_search 경로 — 죽은 분기 제거 ✅ (에이전틱 도구로 재설계 예정)

**현황**
- 라우터가 `route="web_search"`로 분기할 수 있고(`router.py`), 빌더가 노드를 등록(`builder.py`: `web_search → generate`)하며, `web_search_node`는 `Retriever`를 주입받아 호출한다(`nodes/web_search.py`).
- 그러나 **프로덕션 조립부(`app/api/chat.py`)에서 `web_search_retriever`가 주입되지 않는다.** `build_graph()`의 선택적 파라미터로만 존재하고 기본값 `None`. 즉 실제로 이 경로를 타면 `web_search_node`는 빈 문서를 반환한다 → 사실상 **죽은 분기**(테스트에서만 동작).
- core에는 `TavilyRetriever`(`tavily-python`), `DuckDuckGoRetriever`(`duckduckgo-search`) 어댑터가 구현되어 있다.

**문제**
- 라우터·엣지·State Literal·빌더·테스트에 web_search 분기 비용이 분산되어 있으나 실 사용자 가치는 0이다(미연결).
- 현재 구조(`router`의 독립 route 분기 → `web_search_node` → `generate`, grade·재시도 루프 없음)는 **향후 에이전틱 패턴과 맞지 않는다.** 에이전트는 웹검색을 "별도 라우팅 경로"가 아니라 `tool_call` 경로의 **도구 하나**로 다루는 것이 자연스럽다(이미 `confirm`→`tool_executor` 골격 존재).

**조치 (완료 — `feat/remove-web-search-route`)**
- **그래프 경로만 제거 완료**: `router`의 web_search 분기·프롬프트, `builder`의 노드·엣지 등록, `state.py`의 `route` Literal에서 `web_search`, `nodes/web_search.py`, 관련 테스트(`test_web_search.py` 및 `test_builder.py`의 web_search 시나리오). 부수로 `tests/eval/questions.yaml`·`tests/load/locustfile.py`의 web_search 시나리오, 현행 문서(README·requirements·CLAUDE.md) 진술도 정리. (`edges.py`의 `route_after_router`는 route 값을 그대로 반환할 뿐 web_search 리터럴이 없어 변경 불필요였음.)
- **자산 보존**: `core/retriever/adapters/`의 `TavilyRetriever`·`DuckDuckGoRetriever`와 `tavily-python`·`duckduckgo-search` 의존성은 **유지**(CLAUDE.md 규칙 5: 미연결 구현체 삭제 금지). 에이전틱 단계에서 도구로 재사용.
- **에이전틱 재설계 시점**(다음 단계): 웹검색을 `tool_executor`가 호출하는 tool로 등록하고, 필요 시 HITL 승인 흐름에 편입.

### 9.2 langchain 의존성 검토 보고 (제거는 보류)

요청에 따라 **검토·보고만** 하고 실제 제거는 하지 않는다.

**필수 (제거 시 아키텍처 붕괴)**
- `langgraph` — `StateGraph`, `START/END`, `CompiledStateGraph`, `interrupt()`(HITL), `MemorySaver`. 그래프 자체.
- `langgraph-checkpoint-postgres` — `AsyncPostgresSaver`, `JsonPlusSerializer`(`app/api/chat.py`). 상태/세션 지속성.
- `langchain-core` — `RunnableConfig`(`generate.py`의 token_queue 접근 타입), 그리고 `core/`의 LangChain 어댑터가 쓰는 `Document`/`BaseRetriever`/`BaseLLM`. (어댑터 자체는 현재 미연결이나 규칙 5로 보존 대상.)

**미사용 (이론상 제거 가능)**
- `langchain-anthropic`, `langchain-openai` — `core/llm/factory.py`의 `create_chat_llm()`에서만 import되는데, **이 함수가 코드베이스 어디에서도 호출되지 않는다.** 향후 `bind_tools` 기반 에이전트 도입 시 필요해질 수 있음.
- `langchain` (base) — 직접 import 없음(간접 의존만).

**결론**: "langchain 전부 제거"는 불가 — `langgraph` 일가와 `langchain-core`는 핵심 의존이다. 안전하게 줄일 수 있는 건 `langchain-anthropic`/`langchain-openai`(+미사용 `create_chat_llm()`)뿐이며, 이는 에이전틱 로드맵(9.1, 도구 바인딩)과 직접 얽히므로 **에이전틱 설계 확정 후 함께 판단**하는 것을 권장. 현 시점에서는 보류.

### 9.3 기타 관찰된 과제
- **rate limit 인메모리**: 다중 인스턴스 배포 시 분산 환경에서 부정확. 운영 확장 시 공유 저장소(예: DB/Redis) 재검토 여지.
- **권한 캐시 무효화**: 부서 멤버 변경(`POST /admin/.../departments/...`) 후 TTL 만료 전 반영 지연 가능 — 즉시 무효화 경로 점검 권장.

---

## 10. 향후 로드맵 / 다음 기획 후보

| 순서 | 항목 | 근거 |
|------|------|------|
| ✅ | ~~web_search 그래프 경로 제거~~ (완료, 자산 보존) | 9.1 — 죽은 분기 정리, 라우터 단순화 |
| 2 | **에이전틱 도구 실행 정식화** | `tool_call` 경로를 자율 도구 루프로 확장, 웹검색을 tool로 편입 |
| 3 | **langchain-anthropic/openai 제거 판단** | 9.2 — 에이전틱 도구 바인딩 설계 확정 후 |
| 4 | reranker 지표·LLMReranker 고도화 | 백로그(PR #10·#11 머지 이력) |
| 5 | 운영 확장(분산 rate limit, 캐시 무효화) | 9.3 |

> 본 역기획서는 기획 추가·수정 시 함께 갱신한다. 변경이 설계·방향·도구 선택을 동반하면 `docs/superpowers/decisions/`에 ADR을 남긴다.
