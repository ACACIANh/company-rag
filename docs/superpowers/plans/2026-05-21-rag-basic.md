# 기본 RAG 그래프 (rag_basic) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `graphs/rag_basic.py`에 LangGraph 기반 `retrieve → generate` 2노드 RAG 그래프를 추가하고, 단위 테스트 4개 + eval baseline 측정 스크립트를 함께 제공한다.

**Architecture:** 단일 파일 `graphs/rag_basic.py` 안에 State / Nodes / Graph / Eval adapter 4개 섹션을 명확히 구획화한다. 노드는 `(state, *, dep) -> dict` 형태의 순수 함수이며 의존성(retriever/llm)은 `build_graph()`에서 `functools.partial`로 바인딩한다. LangGraph 의존은 `graphs/` 안에만 둔다 (shared/ 무침범).

**Tech Stack:** Python 3.11+, langgraph>=0.2.0, langchain-core (HumanMessage/AIMessage), pytest

**Spec:** `docs/superpowers/specs/2026-05-21-rag-basic-design.md`

---

## File Structure

| 파일 | 책임 | 작업 |
|---|---|---|
| `graphs/__init__.py` | 패키지 마커 | Create |
| `graphs/rag_basic.py` | State, 2 노드, build_graph, answer_question | Create |
| `tests/test_graphs/__init__.py` | 테스트 패키지 마커 | Create |
| `tests/test_graphs/test_rag_basic.py` | 노드/그래프/어댑터 단위 테스트 + Fake 정의 | Create |
| `scripts/eval_rag_basic.py` | baseline 점수 측정 진입 스크립트 | Create |
| `CLAUDE.md` | ADR 테이블의 RAG 행 갱신 | Modify (1줄) |

---

## Task 1: 디렉터리 골격 만들기

**Files:**
- Create: `graphs/__init__.py`
- Create: `tests/test_graphs/__init__.py`

- [ ] **Step 1: 패키지 마커 두 개 생성**

`graphs/__init__.py` — 빈 파일.
`tests/test_graphs/__init__.py` — 빈 파일.

- [ ] **Step 2: import 가능 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -c "import graphs; import tests.test_graphs; print('ok')"`
Expected: `ok`

- [ ] **Step 3: 커밋**

```bash
git add graphs/__init__.py tests/test_graphs/__init__.py
git commit -m "chore: add graphs/ and tests/test_graphs/ package markers"
```

---

## Task 2: RagState + retrieve_node (TDD)

**Files:**
- Create: `graphs/rag_basic.py` (State + retrieve_node만)
- Create: `tests/test_graphs/test_rag_basic.py` (Fakes + retrieve 테스트)

- [ ] **Step 1: Fakes와 첫 실패 테스트 작성**

`tests/test_graphs/test_rag_basic.py`:

```python
from langchain_core.messages import HumanMessage

from shared.llm.base import LLMClient
from shared.models import Chunk, SearchResult
from shared.retriever.base import Retriever

from graphs.rag_basic import retrieve_node


class FakeRetriever(Retriever):
    def __init__(self, results: list[SearchResult]) -> None:
        self._results = results
        self.last_query: str | None = None
        self.last_top_k: int | None = None

    def retrieve(self, query: str, top_k: int = 5) -> list[SearchResult]:
        self.last_query = query
        self.last_top_k = top_k
        return self._results


class FakeLLM(LLMClient):
    def __init__(self, response: str = "fake answer") -> None:
        self._response = response
        self.last_prompt: str | None = None

    def complete(self, prompt: str) -> str:
        self.last_prompt = prompt
        return self._response


def _sr(text: str, source: str, chunk_id: str = "c1", score: float = 0.9) -> SearchResult:
    return SearchResult(
        chunk=Chunk(text=text, source=source, chunk_id=chunk_id),
        score=score,
    )


def test_retrieve_node_calls_retriever_with_last_message():
    fake = FakeRetriever([_sr("hello", "s.md")])
    state = {"messages": [HumanMessage(content="question?")]}

    result = retrieve_node(state, retriever=fake)

    assert fake.last_query == "question?"
    assert fake.last_top_k == 5
    assert len(result["retrieved_docs"]) == 1
    assert result["retrieved_docs"][0].chunk.source == "s.md"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m pytest tests/test_graphs/test_rag_basic.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'graphs.rag_basic'` 또는 `ImportError`

- [ ] **Step 3: State + retrieve_node 최소 구현**

`graphs/rag_basic.py`:

```python
"""기본 RAG 그래프: retrieve → generate 2노드 선형 그래프.

내부 섹션 구획 (추후 추출 친화):
  - State  → state/rag_state.py
  - Nodes  → nodes/retrieve.py, nodes/generate.py
  - Graph assembly
  - Eval adapter → graphs/adapters.py
"""

from langgraph.graph import MessagesState

from shared.models import SearchResult
from shared.retriever.base import Retriever


# ===== State =====


class RagState(MessagesState):
    retrieved_docs: list[SearchResult]


# ===== Nodes =====


def retrieve_node(state: RagState, *, retriever: Retriever) -> dict:
    query = state["messages"][-1].content
    results = retriever.retrieve(query, top_k=5)
    return {"retrieved_docs": results}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m pytest tests/test_graphs/test_rag_basic.py::test_retrieve_node_calls_retriever_with_last_message -v`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add graphs/rag_basic.py tests/test_graphs/test_rag_basic.py
git commit -m "feat(graphs): add RagState and retrieve_node for rag_basic"
```

---

## Task 3: generate_node (TDD)

**Files:**
- Modify: `graphs/rag_basic.py` (generate_node 추가)
- Modify: `tests/test_graphs/test_rag_basic.py` (generate 테스트 추가)

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_graphs/test_rag_basic.py` 끝에 다음 추가하고, 상단 import에 `AIMessage`와 `generate_node` 추가:

상단 import 수정:
```python
from langchain_core.messages import AIMessage, HumanMessage
```
```python
from graphs.rag_basic import generate_node, retrieve_node
```

테스트 추가:
```python
def test_generate_node_appends_aimessage_with_context():
    fake = FakeLLM(response="final answer")
    docs = [_sr("context-text", "s.md")]
    state = {
        "messages": [HumanMessage(content="question?")],
        "retrieved_docs": docs,
    }

    result = generate_node(state, llm=fake)

    assert "context-text" in fake.last_prompt
    assert "question?" in fake.last_prompt
    assert len(result["messages"]) == 1
    assert isinstance(result["messages"][0], AIMessage)
    assert result["messages"][0].content == "final answer"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m pytest tests/test_graphs/test_rag_basic.py::test_generate_node_appends_aimessage_with_context -v`
Expected: FAIL — `ImportError: cannot import name 'generate_node'`

- [ ] **Step 3: generate_node 구현**

`graphs/rag_basic.py` 상단 import에 추가:
```python
from langchain_core.messages import AIMessage

from shared.llm.base import LLMClient
```

`# ===== Nodes =====` 섹션 안 `retrieve_node` 아래에 추가:
```python
def generate_node(state: RagState, *, llm: LLMClient) -> dict:
    question = state["messages"][-1].content
    context = "\n\n".join(d.chunk.text for d in state["retrieved_docs"])
    prompt = f"context:\n{context}\n\nquestion: {question}\nanswer in Korean."
    text = llm.complete(prompt)
    return {"messages": [AIMessage(content=text)]}
```

- [ ] **Step 4: 두 노드 테스트 모두 통과 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m pytest tests/test_graphs/test_rag_basic.py -v`
Expected: 2 passed

- [ ] **Step 5: 커밋**

```bash
git add graphs/rag_basic.py tests/test_graphs/test_rag_basic.py
git commit -m "feat(graphs): add generate_node for rag_basic"
```

---

## Task 4: build_graph 조립 (TDD, end-to-end)

**Files:**
- Modify: `graphs/rag_basic.py` (build_graph 추가)
- Modify: `tests/test_graphs/test_rag_basic.py` (end-to-end 테스트 추가)

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_graphs/test_rag_basic.py` 상단 import 수정 — `build_graph` 추가:
```python
from graphs.rag_basic import build_graph, generate_node, retrieve_node
```

테스트 추가:
```python
def test_build_graph_end_to_end_with_fakes():
    fake_r = FakeRetriever([_sr("ctx", "x.md")])
    fake_l = FakeLLM(response="final")

    graph = build_graph(fake_r, fake_l)
    final = graph.invoke({"messages": [HumanMessage(content="q?")]})

    assert len(final["retrieved_docs"]) == 1
    assert final["retrieved_docs"][0].chunk.source == "x.md"
    last = final["messages"][-1]
    assert isinstance(last, AIMessage)
    assert last.content == "final"
    # retriever가 마지막 user message로 검색되었는지
    assert fake_r.last_query == "q?"
    # llm prompt에 context 포함
    assert "ctx" in fake_l.last_prompt
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m pytest tests/test_graphs/test_rag_basic.py::test_build_graph_end_to_end_with_fakes -v`
Expected: FAIL — `ImportError: cannot import name 'build_graph'`

- [ ] **Step 3: build_graph 구현**

`graphs/rag_basic.py` import 수정:
- 새로 추가: `from functools import partial`
- 새로 추가: `from langgraph.graph.state import CompiledStateGraph`
- 기존 `from langgraph.graph import MessagesState` 줄을 다음으로 교체: `from langgraph.graph import END, START, MessagesState, StateGraph`

기존 `# ===== Nodes =====` 섹션 아래에 새 섹션 추가:
```python
# ===== Graph assembly =====


def build_graph(retriever: Retriever, llm: LLMClient) -> CompiledStateGraph:
    g = StateGraph(RagState)
    g.add_node("retrieve", partial(retrieve_node, retriever=retriever))
    g.add_node("generate", partial(generate_node, llm=llm))
    g.add_edge(START, "retrieve")
    g.add_edge("retrieve", "generate")
    g.add_edge("generate", END)
    return g.compile()
```

- [ ] **Step 4: 모든 테스트 통과 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m pytest tests/test_graphs/test_rag_basic.py -v`
Expected: 3 passed

- [ ] **Step 5: 커밋**

```bash
git add graphs/rag_basic.py tests/test_graphs/test_rag_basic.py
git commit -m "feat(graphs): assemble rag_basic graph via build_graph()"
```

---

## Task 5: answer_question 어댑터 (TDD)

**Files:**
- Modify: `graphs/rag_basic.py` (answer_question 추가)
- Modify: `tests/test_graphs/test_rag_basic.py` (어댑터 테스트 추가)

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_graphs/test_rag_basic.py` 상단 import 수정 — `Answer`, `answer_question` 추가:
```python
from shared.models import Answer, Chunk, SearchResult
```
```python
from graphs.rag_basic import answer_question, build_graph, generate_node, retrieve_node
```

테스트 추가:
```python
def test_answer_question_returns_answer_dto():
    fake_r = FakeRetriever([
        _sr("a", "a.md", chunk_id="c1"),
        _sr("b", "b.md", chunk_id="c2", score=0.8),
    ])
    fake_l = FakeLLM(response="answer")
    graph = build_graph(fake_r, fake_l)

    ans = answer_question(graph, "q?")

    assert isinstance(ans, Answer)
    assert ans.text == "answer"
    assert ans.sources == ["a.md", "b.md"]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m pytest tests/test_graphs/test_rag_basic.py::test_answer_question_returns_answer_dto -v`
Expected: FAIL — `ImportError: cannot import name 'answer_question'`

- [ ] **Step 3: answer_question 구현**

`graphs/rag_basic.py` import 수정 (이미 존재하는 항목은 건드리지 말 것):
- 기존 `from langchain_core.messages import AIMessage` 줄을 다음으로 교체: `from langchain_core.messages import AIMessage, HumanMessage`
- 기존 `from shared.models import SearchResult` 줄을 다음으로 교체: `from shared.models import Answer, SearchResult`

파일 끝에 새 섹션 추가:
```python
# ===== Eval adapter =====


def answer_question(graph: CompiledStateGraph, question: str) -> Answer:
    final = graph.invoke({"messages": [HumanMessage(content=question)]})
    sources = [d.chunk.source for d in final["retrieved_docs"]]
    return Answer(text=final["messages"][-1].content, sources=sources)
```

- [ ] **Step 4: 전체 테스트 통과 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m pytest tests/test_graphs/test_rag_basic.py -v`
Expected: 4 passed

- [ ] **Step 5: 전체 회귀 (graphs 외 깨진 곳 없는지)**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m pytest -q`
Expected: 전체 PASS, 신규 4 추가됨, 기존 테스트 깨지지 않음

- [ ] **Step 6: 커밋**

```bash
git add graphs/rag_basic.py tests/test_graphs/test_rag_basic.py
git commit -m "feat(graphs): add answer_question eval adapter for rag_basic"
```

---

## Task 6: eval baseline 측정 스크립트 + 실행

**Files:**
- Create: `scripts/eval_rag_basic.py`

- [ ] **Step 1: 스크립트 작성**

`scripts/eval_rag_basic.py`:

```python
"""rag_basic 그래프 baseline 점수 측정.

사용법:
    python -m scripts.eval_rag_basic

선행 조건:
    python -m scripts.build_index  # 벡터 저장소가 비어있다면
"""

import os
import sys

_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, _ROOT)

from shared.config import load_config
from shared.embedder import SentenceTransformerEmbedder
from shared.llm.factory import create_llm
from shared.retriever import BasicRetriever
from shared.vector_store.factory import create_vector_store

from graphs.rag_basic import answer_question, build_graph
from eval_suite.runner import run_eval


def main() -> None:
    config = load_config()
    embedder = SentenceTransformerEmbedder(config.embedding_model)
    store = create_vector_store(config)
    retriever = BasicRetriever(store=store, embedder=embedder)
    llm = create_llm(config)

    graph = build_graph(retriever, llm)
    run_eval(lambda q: answer_question(graph, q))


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: (사전 점검) 벡터 저장소 인덱싱 상태 확인**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -c "from shared.config import load_config; from shared.vector_store.factory import create_vector_store; s = create_vector_store(load_config()); print(s)"`
Expected: 에러 없이 출력. 만약 인덱싱이 비어있다고 의심되면 다음 줄을 실행:

Run (필요시): `/Users/acacian/vscode/company-rag/.venv/bin/python -m scripts.build_index`
Expected: `인덱싱 완료: N개 청크 (...)`

- [ ] **Step 3: eval 스크립트 실행 및 결과 캡처**

Run: `/Users/acacian/vscode/company-rag/.venv/bin/python -m scripts.eval_rag_basic 2>&1 | tee /tmp/rag_basic_baseline.txt`
Expected: 5개 질문 각각의 `recall@5`, `keyword_hit`, `src` 출력, 마지막에 `Aggregate: {...}`

결과의 `Aggregate` 줄을 복사해 둔다 (PR 본문 / Task 7의 ADR 커밋 메시지에 첨부).

- [ ] **Step 4: 커밋 (결과 메시지에 baseline 첨부)**

Aggregate 값을 채워 커밋:

```bash
git add scripts/eval_rag_basic.py
git commit -m "feat(scripts): add baseline eval script for rag_basic

baseline:
  mean_recall_at_k=<값>
  mean_keyword_hit_rate=<값>
  n_cases=5
  n_errors=<값>
note: retry/timeout 미적용 (다음 PR에서 보강 예정)"
```

(만약 `n_errors > 0` 이라면 메시지에 원인 1줄 요약 추가 — 예: LLM 키 미설정, 벡터스토어 비어있음 등.)

---

## Task 7: CLAUDE.md ADR 갱신

**Files:**
- Modify: `CLAUDE.md:26` (RAG 행)

- [ ] **Step 1: ADR 테이블 RAG 행 수정**

`CLAUDE.md` 26번째 줄:

```
| RAG | 기본 워크플로우 → 추후 고급 RAG | `docs/langgraph-guide/04-rag.md` |
```

를 다음으로 바꾼다:

```
| RAG | 기본 워크플로우 (`graphs/rag_basic.py`) → 추후 고급 RAG | `docs/langgraph-guide/04-rag.md` |
```

- [ ] **Step 2: 변경 확인**

Run: `git diff CLAUDE.md`
Expected: RAG 행만 변경된 1-line diff

- [ ] **Step 3: 커밋**

```bash
git add CLAUDE.md
git commit -m "docs(adr): point RAG row to graphs/rag_basic.py"
```

---

## 완료 체크리스트 (DoD)

- [ ] `graphs/rag_basic.py` 존재 — State, retrieve_node, generate_node, build_graph, answer_question 모두 export
- [ ] `tests/test_graphs/test_rag_basic.py` 4개 테스트 모두 PASS
- [ ] 전체 `pytest -q` 통과 (기존 회귀 없음)
- [ ] `scripts/eval_rag_basic.py` 실행해 baseline 점수 확보 → 커밋 메시지에 첨부
- [ ] `CLAUDE.md` ADR 테이블 갱신
- [ ] 커밋 7개 (Task 1~7 각 1개) 또는 동등한 단위로 분리되어 있음

## 위험 / 주의

- **retry/timeout 미적용**: 의도된 단순화. spec §7에 명시되어 있음. Task 6 커밋 메시지에 ADR 위반 사실 명시.
- **벡터 저장소가 비어있는 경우**: Task 6 Step 2에서 `python -m scripts.build_index` 선행 실행 필요.
- **API 키 미설정**: `.env`에 `ANTHROPIC_API_KEY` 또는 `OPENAI_API_KEY` 가 없으면 Task 6에서 5/5 에러로 나옴 — 이 경우 baseline은 "환경 미구성으로 측정 불가"로 기록하고 별도 작업으로 분리.
